programa soma inicio
    real a;
    inteiro b;
    inteiro c;
    booleano e;
    real d;
    e = verdadeiro;
    b = 3;
    d = 2 + a * b / a;

    e = (1 < 2) & 2 < 3 | 1 < 0;
    //escreva(e);

    enquanto (a < b & b != 0) inicio
        a = a + 1;
    fim;

    se (1 != a) inicio
        escreva(d);
    fim;
fim.